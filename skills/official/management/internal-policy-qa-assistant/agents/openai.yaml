interface:
  display_name: "내부 규정 Q&A 응답 보조"
  short_description: "복무, 구매, 재무 규정 문서를 검색해 직원 문의에 근거 문단과 함께 답변 초안을 제공합니다."
  default_prompt: "Use $internal-policy-qa-assistant to prepare a practical internal work draft."

policy:
  allow_implicit_invocation: true
